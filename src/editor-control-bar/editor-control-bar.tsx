import React from "react";

const EditorControlBar: React.ElementType<React.JSX.Element> = (props: any) => {
return(
  <div>
    THIS IS THE EDITOR CONTROL BAR!
  </div>);
};

export default EditorControlBar;